Change user password.
---
tags:
  - User
consumes:
    - multipart/form-data
parameters:
    - in: formData
      name: token
      type: string
      require: true
      description: The token.
    - in: formData
      name: password
      type: string
      require: true
      description: the old password.
    - in: formData
      name: newpassword
      type: string
      require: true
      description: the new password.
responses:
  200:
    description: Change User password successful!
